# Backup Storage Monitor Configuration
# Configure your email settings here

# Email address to receive backup alerts
ADMIN_EMAIL=webmaster@comdat.ca

# SMTP Configuration
# For local mail: localhost:25 (no authentication needed)
# For Gmail: smtp.gmail.com:587 (with app password)
# For AWS SES: email-smtp.region.amazonaws.com:587
SMTP_SERVER=localhost
SMTP_PORT=25
SENDER_EMAIL=nexus@localhost

# Optional authentication (leave empty for local mail)
SMTP_USER=
SMTP_PASSWORD=
