# Backup Storage Monitor Configuration
# This file contains environment variables for the backup storage check script

# Admin email address to receive alerts
ADMIN_EMAIL=admin@example.com

# SMTP Configuration
# For local sendmail/postfix: use "localhost" and port 25
# For Gmail: use "smtp.gmail.com" and port 587 with authentication
# For AWS SES: use "email-smtp.region.amazonaws.com" and port 587

SMTP_SERVER=localhost
SMTP_PORT=25
SENDER_EMAIL=nexus@hostname

# Optional: SMTP authentication (leave empty for local mail systems)
SMTP_USER=
SMTP_PASSWORD=

# Storage Configuration (GB)
# These are hardcoded in the script but can be adjusted if needed:
# TOTAL_STORAGE_GB=20
# SAFETY_MARGIN_GB=0.5
